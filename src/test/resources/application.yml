spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_auth_service_test
    username: yunfei
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

adp:
  auth:
    api-key: dev-internal-key
    token-ttl-hours: 72
